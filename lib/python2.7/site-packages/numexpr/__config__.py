# This file is generated by /opt/concourse/worker/volumes/live/d58bc37c-93d0-4b36-620e-0aa522f24500/volume/numexpr_1512071619533/work/setup.py
# It contains system_info results at the time of building this package.
__all__ = ["get_info","show"]

mkl_info={'libraries': ['mkl_rt', 'pthread'], 'library_dirs': ['/anaconda2/lib'], 'define_macros': [('SCIPY_MKL_H', None)], 'include_dirs': ['/anaconda2/include']}

def get_info(name):
    g = globals()
    return g.get(name, g.get(name + "_info", {}))

def show():
    for name,info_dict in globals().items():
        if name[0] == "_" or type(info_dict) is not type({}): continue
        print(name + ":")
        if not info_dict:
            print("  NOT AVAILABLE")
        for k,v in info_dict.items():
            v = str(v)
            if k == "sources" and len(v) > 200:
                v = v[:60] + " ...\n... " + v[-60:]
            print("    %s = %s" % (k,v))
    